# Задание 012 — сменить счётчик Метрики на сайте

Статус: не выполнено

На сайте по-прежнему стоит счётчик 110459313. Причина та же, что и у заданий
009 и 010: с сервера не проходит ни одно TCP-соединение до Бегета — ни SSH, ни
HTTPS. Разбор блокировки с цифрами — в `outbox/009-20260905-1242.md`. Правка
написана целиком и лежит в `packages/mu-plugins/`, залить её нельзя.

Задание пришло в inbox уже после начала прогона (коммит 9b334b8), поэтому идёт
последним, а не первым, как просит его приоритет. Первым его всё равно было бы
не сделать — доступа не было и в 12:20.

## Что сделано

**1. Найдены все места с номером 110459313.** Искал не только там, где задание
ожидало. Живой `grep` по сайту запустить нельзя, поэтому источники — офсайт-бэкап
`files-20260905-0320.tar.gz` (mu-плагины, `.htaccess`, `robots.txt`, `tools/`,
`packages/`) и дамп базы `db-20260905-0320.sql.zst`:

| где искал | найдено |
|---|---|
| `wp-content/mu-plugins/` | **3 вхождения в 2 файлах** |
| `.htaccess`, `robots.txt` | нет |
| `tools/`, `packages/` на Бегете | нет |
| `wp_options` (аналог `wp db query … LIKE '%110459313%'`) | нет |
| `wp_posts.post_content` | нет |
| `wp_postmeta` | нет |

Три вхождения:

```
mu-regionzhbi-setup.php:10  define('REGIONZHBI_METRIKA_ID', 110459313)   ← рабочее
mu-rz-celi.php:31           …: (int) REGIONZHBI_METRIKA_ID : 110459313   ← запасное
mu-rz-celi.php:24           комментарий с номером
```

Ничего неожиданного не всплыло. Номер и так объявлен по одному разу на файл
константой, как требует пункт 2 задания, — рассыпанным по тексту он не был:
в `mu-regionzhbi-setup.php` в скрипт подставляется `$id`, в `mu-rz-celi.php` —
`RZ_CELI_METRIKA_ID`. Связь между файлами тоже уже сделана правильно:
`mu-rz-celi.php` берёт номер из `REGIONZHBI_METRIKA_ID`, если тот определён, и
свой запасной использует, только если `mu-regionzhbi-setup.php` пропадёт.
Практически это значит, что рабочая правка — одна строка, остальные две
приводятся в порядок, чтобы старый номер не остался в файлах вообще.

**2. Заменено на 112302967** в обоих файлах, включая комментарии. Старого номера
в подготовленных файлах нет ни одного вхождения — проверка задания
(`grep -rn '110459313' wp-content/mu-plugins/` не находит ничего) на них
проходит. Историю смены в коде я намеренно не оставил цифрой: она в этом отчёте
и в коммите, а в коде номер должен быть один.

**3. Убрана отправка цели `call`.** В `mu-regionzhbi-setup.php` удалена строка

```
if(a.href&&a.href.indexOf('tel:')===0)ym(<?php echo $id; ?>,'reachGoal','call');
```

Больше в этом плагине не тронуто ничего: цели `messenger` и `price` на месте,
стили и Organization-schema не трогались. Клик по `tel:` теперь шлёт одно
событие — цель `tel` из `mu-rz-celi.php`.

Итого правка: **2 файла, 3 замены номера и 1 удалённая строка.**

| файл | было | стало |
|---|---|---|
| `mu-regionzhbi-setup.php` | md5 `5e0dd1f81f9d355cc625504384d922ba`, 4 417 байт | md5 `c3c05ea12562e16b5448a5ecd60e08c5`, 5 061 байт |
| `mu-rz-celi.php` | md5 `3eb2f2542c4c198ed712a6b35ed3a06a`, 8 501 байт | md5 `484910ee1cf4a1d8fe431c32c16a8b39`, 8 668 байт |

`mu-rz-celi.php` в репозитории до правки совпадал с тем, что стоит на сайте,
байт в байт (md5 сверен с копией из бэкапа) — правил я актуальную версию, а не
устаревшую.

## Проверка

**Пункт 4 задания — «проверить живьём, а не по коду» — не выполнен.** Открыть
главную и карточку товара с этого сервера нельзя. Писать, что номер в исходном
коде страницы сменился, я не буду: я этого не видел.

Что проверено:

- `grep -rn '110459313' packages/mu-plugins/` — пусто;
- `grep -rn '112302967' packages/mu-plugins/` — 5 вхождений: по одному рабочему
  на файл (`define` и запасное значение) и три в пояснениях;
- `grep -rn "reachGoal','call'"` — пусто;
- дифф обоих файлов против исходников из бэкапа глазами: ровно те правки, что
  описаны выше, и ничего сверх.

`php -l` не запускался: `php-cli` на сервере нет, root для установки тоже.
Балансовая проверка `/srv/regiongbi/tmp/w009/phpcheck.py` на этих двух файлах
неприменима — они чередуют `?>` и `<?php` с HTML и JavaScript, и проверка
ругается на них **одинаково до и после правки** (прогнал на исходниках для
контроля). Поэтому единственная реальная гарантия здесь — маленький дифф;
`php -l` на Бегете обязателен до включения файлов.

## Не сделано и почему

**Не залито ничего.** Порядок, когда доступ вернётся:

```
bash /srv/regiongbi/bin/backup.sh
# живой grep — задание требует именно его, бэкап темы не содержит:
ssh … "cd <public_html> && grep -rn 110459313 wp-content/mu-plugins/ wp-content/themes/ .htaccess"
ssh … "cd <public_html> && wp db query \"SELECT option_name FROM wp_options WHERE option_value LIKE '%110459313%'\""
scp -i <ключ> packages/mu-plugins/mu-regionzhbi-setup.php packages/mu-plugins/mu-rz-celi.php \
    pkfgbi8c@pkfgbi8c.beget.tech:/home/p/pkfgbi8c/regiongbi.ru/public_html/wp-content/mu-plugins/
ssh … "cd <public_html> && php -l wp-content/mu-plugins/mu-regionzhbi-setup.php \
        && php -l wp-content/mu-plugins/mu-rz-celi.php && wp cache-enabler clear"
bash /srv/regiongbi/bin/monitor.sh -v
curl -s -H 'Accept: text/html' https://regiongbi.ru/ | grep -c 112302967      # должно быть >0
curl -s -H 'Accept: text/html' https://regiongbi.ru/ | grep -c 110459313      # должно быть 0
curl -s -H 'Accept: text/html' https://regiongbi.ru/catalog/koltsa-kolodeznye-ks/ks-10-9/ | grep -c 112302967
```

**Тема не проверена.** Задание просит `grep -rn '110459313' wp-content/themes/`,
а `backup.sh` тему в архив не кладёт (и правильно — правки темы затирает
обновление). Так что «нашёл все места» я утверждаю про mu-плагины, `.htaccess`
и базу, но **не про тему**. Проверить это — первый шаг при заливке, команда
выше. Если номер найдётся в теме, править его там нельзя по правилу AGENTS.md —
это отдельный разговор с владельцем.

**Что нужно и от кого.** Разблокировка IP `147.45.221.57` в поддержке Бегета —
владелец, текст обращения в отчёте 009. Больше ничего: цели в новом счётчике,
по тексту задания, уже заведены.

## Замечено попутно

1. **Цель `price` после смены счётчика начнёт задваиваться, как только на сайте
   появится файл прайса.** Её шлют оба плагина: `mu-regionzhbi-setup.php`
   строка 56 — по условию «`prajs` или `price` где угодно в адресе» и по классу
   `pr-dl`, и `mu-rz-celi.php` — по своему условию. Раньше это было безобидно
   (в старом счётчике целей не было вовсе), теперь цель `price` в счётчике
   112302967 есть, и один клик даст два достижения. Задание 012 разрешает
   трогать в `mu-regionzhbi-setup.php` только цель `call`, поэтому я не трогал.
   **Это надо решить до того, как прайс появится на сайте**, иначе цифра
   «открыт прайс» будет вдвое завышена и никто потом не поймёт почему. Развести
   их — работа облачной сессии: сказать, какой из двух плагинов остаётся
   ответственным за `price`.

2. **Цель `messenger` в новом счётчике не заведена.** В задании перечислены
   четыре цели — `zayavka`, `tel`, `mail`, `price`; `messenger` шлётся из
   `mu-regionzhbi-setup.php` строка 55 и теперь уходит в никуда. Вреда нет,
   но либо цель стоит завести в кабинете, либо вызов убрать — как с `call`.
   Решает облачная сессия, я не трогал.

3. **В `mu-rz-celi.php` цели описаны как «JavaScript-событие», в задании 012 —
   как «Целевое событие».** Это одно и то же в кабинете Метрики, разные
   названия в разных версиях интерфейса. Идентификаторы совпадают
   (`zayavka`, `tel`, `mail`, `price`), так что расхождения по сути нет.

4. **Ловушка rebase из отчёта 007 сработала снова, и именно на этом задании.**
   Пока я работал, в inbox прилетело задание 012. При `git rebase origin/main`
   git увидел, что все файлы `inbox/` переехали в `done/`, счёл это
   переименованием каталога и предложил положить туда же новое задание 012 —
   то есть пометить невыполненную работу выполненной. Конфликт разрешён
   вручную, задание возвращено в `inbox/`. Для будущих прогонов: **после
   любого rebase проверять `ls inbox/` глазами** — молчаливой ошибки здесь не
   будет видно ни в статусе, ни в логе.

## Откат

Откатывать нечего: на сайте ничего не менялось, бэкап не снимался.

Если правку зальют и она окажется неудачной — вернуть оба файла из офсайт-бэкапа:

```
tar xzf /srv/regiongbi/backups/2026-09-05/files-20260905-0320.tar.gz -C /tmp \
    public_html/wp-content/mu-plugins/mu-regionzhbi-setup.php \
    public_html/wp-content/mu-plugins/mu-rz-celi.php
scp -i <ключ> /tmp/public_html/wp-content/mu-plugins/mu-regionzhbi-setup.php \
              /tmp/public_html/wp-content/mu-plugins/mu-rz-celi.php \
    pkfgbi8c@pkfgbi8c.beget.tech:/home/p/pkfgbi8c/regiongbi.ru/public_html/wp-content/mu-plugins/
ssh … "cd <public_html> && wp cache-enabler clear"
```

Прежние файлы: `mu-regionzhbi-setup.php` md5 `5e0dd1f81f9d355cc625504384d922ba`,
`mu-rz-celi.php` md5 `3eb2f2542c4c198ed712a6b35ed3a06a`.
База не затрагивается — правка только в файлах.
